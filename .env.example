# ═══════════════════════════════════════════════════════════════════
# Restaurant CMS — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ═══════════════════════════════════════════════════════════════════

# ── Database (Neon PostgreSQL) ───────────────────────────────────────────────
# 1. Create a project at https://neon.tech
# 2. Enable "Connection Pooling" in the Neon dashboard (Connection Details tab)
# 3. Copy the pooled connection string
DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/restaurant?sslmode=require

# ── Payload CMS ──────────────────────────────────────────────────────────────
# Secret key for JWT signing and data encryption.
# Generate: openssl rand -base64 64
PAYLOAD_SECRET=your-very-long-secret-key-at-least-32-characters

# The full public URL of your site (no trailing slash)
# Development: http://localhost:3000
# Production: https://your-restaurant.vercel.app
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# ── Email (Resend) ────────────────────────────────────────────────────────────
# 1. Sign up at https://resend.com
# 2. Create an API key
# 3. Verify your sending domain (or use onboarding@resend.dev for testing)
RESEND_API_KEY=re_your_api_key_here

# The verified "From" email address (must match your Resend verified domain)
RESEND_FROM_EMAIL=contact@yourdomain.com

# The restaurant owner's email — contact form submissions are sent here
RESEND_TO_EMAIL=owner@yourdomain.com

# ── On-Demand Revalidation ────────────────────────────────────────────────────
# Secret token to authorize external webhook calls to /api/revalidate
# Generate: openssl rand -hex 32
REVALIDATION_SECRET=your-revalidation-secret-here

# ── Vercel Blob Storage (production only) ─────────────────────────────────────
# Required for media uploads in production.
# Created automatically when you add Vercel Blob to your project in the dashboard.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
